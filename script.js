const vids=[...document.querySelectorAll("video")];const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.play().catch(()=>{});else e.target.pause()}),{threshold:.2});vids.forEach(v=>io.observe(v));document.querySelectorAll(".play").forEach(b=>b.addEventListener("click",e=>{e.stopPropagation();const v=b.parentElement.querySelector("video");if(v.paused){v.play().catch(()=>{});b.textContent="❚❚"}else{v.pause();b.textContent="▶"}}));document.querySelectorAll("img").forEach(img=>img.addEventListener("error",()=>{img.style.display="none";img.parentElement.classList.add("logo-fallback")}));document.querySelectorAll(".portal,.brand-card,.social-card").forEach(el=>{el.addEventListener("pointermove",e=>{if(innerWidth<900)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${(-y*5).toFixed(2)}deg) rotateY(${(x*6).toFixed(2)}deg) translateY(-3px)`});el.addEventListener("pointerleave",()=>el.style.transform="")});